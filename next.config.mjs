/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains:["img.freepik.com", "media.istockphoto.com", "images.unsplash.com", "www.sonypictures.com"]
    }
};

export default nextConfig;
